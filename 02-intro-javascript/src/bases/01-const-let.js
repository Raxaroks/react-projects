// Variables y constantes.
// Ya no tenemos que usar 'var' obligatoriamente en variables. Solamente hay que usar 'const' y 'let'.

const nombre = "Andrés";
let apellido = "Ornelas";

let valorDado = 5;
valorDado = 4;

console.log(nombre, apellido, valorDado);

// Ya no debemos usar 'var' para nada! Es importante que no se nos olvide.
if ( true ) {
    const nombre = "Bella, la Soplavelas";
    let valorDado = 6;
    console.log(valorDado, nombre);
}

console.log(valorDado);

